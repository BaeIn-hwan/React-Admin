import { useEffect, useState } from 'react';

import { ContentContainer } from '@/styled/Layout';

const MainIndex = () => {
  return <ContentContainer className="main"></ContentContainer>;
};

export default MainIndex;
